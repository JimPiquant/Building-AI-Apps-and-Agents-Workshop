[project]
name = "day1-lab"
version = "0.1.0"
description = "Day 1 lab — three ways to run an agent with Foundry."
requires-python = ">=3.11"
dependencies = [
    "agent-framework",
    "agent-framework-foundry",
    "azure-ai-projects",
    "azure-identity",
    "python-dotenv",
]

# 2026 baseline pinning happens via uv.lock (committed).
# See manifests/versions.md for the version policy.

[tool.uv]
package = false

#[[tool.uv.index]]
#name = "internal"
#url = "https://packagefeedproxy.microsoft.io/pypi/simple/"